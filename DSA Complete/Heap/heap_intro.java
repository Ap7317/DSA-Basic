package Heap;
import java.util.*;

// Heap is a Complete Binary Tree
// Max Heap for every node parent must be greater than his all child
// Min Heap for every node parent must be smaller than his all child
// Complete Binary tree means at every level is filled from left to right from starting
// Left Child in a Heap --> 2*idx+1(in Array)
// Right Child in a Heap --> 2*idx+2(in Array)
// Parent of A node in a Heap --> Ceil((i-1)/2)


